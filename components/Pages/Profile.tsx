import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Profile = () => {
  return <Text>Profile Here!</Text>;
};

export default Profile;

const styles = StyleSheet.create({
  header: {
    fontSize: 20,
    padding: 23,
    flex: 1,
  },
});
