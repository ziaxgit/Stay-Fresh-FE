import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Scan = () => {
  return <Text>Scan Here!</Text>;
};

export default Scan;

const styles = StyleSheet.create({
  header: {
    fontSize: 20,
    padding: 23,
    flex: 1,
  },
});
